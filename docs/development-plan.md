# Development plan — Revision 6.1 build sequence

**Status:** locked before Phase 0. Agreed in the planning session that produced Revision 6.1; recorded here so the sequence lives in the repository, not in chat.
**Authority:** this document orders the work. It does not define product behaviour or architecture. Requirements are `spec.md` (§31 acceptance criteria, §32 guardrails), the locked stack is `technology-decisions.md`, renderer and model contracts are `event-renderer-system.md` and `model-contracts.md`. When this plan and those documents disagree, they win and this plan is corrected.
**How work is routed:** `CLAUDE.md §11` (task packets, Haiku → Sonnet → Opus → Fable, one integrated review per meaningful change).

## Principles behind the order

1. **Deterministic core before live AI.** The canonical pipeline requires a `CompositionTree` to pass deterministic compilation and rendered-geometry verification through the production renderer before a `ResolvedDesignSpec` is final. So the compiler/renderer engine (Phase 3) precedes AI orchestration (Phase 4). The engine needs no auth, database or model call, and it already has 46 adversarial fixtures and 72 frozen confirmation trees waiting for it (`../proof-b/`).
2. **Settle the one open runtime question first.** Geometry verification runs headless Chromium against the production renderer before a spec is persisted. The approved options were a Vercel Node runtime with a serverless Chromium build, or a small render worker (settled by the Phase 0 spike verdict in `technology-decisions.md`, "Geometry verification runtime": Vercel serverless Chromium). Client-side verification in the host's browser is **not** an interchangeable fallback; it would change an architectural invariant and requires a deliberate revision.
3. **Incremental physical schema, complete conceptual model.** Migrations arrive with their domain phase, but the whole domain model (`spec.md §24`) and stable identifiers and RLS boundaries are understood before Phase 1 so later domains do not collide with early assumptions.
4. **Spend controls ship with the first production model call**, not in hardening. The proof hit an organisation spend ceiling mid-run. Requirements: one batch in flight per event, per-event and per-account daily caps, global ceiling and alerts, rate limits, idempotency, usage telemetry (`spec.md` generation and spend limits).
5. **Human design-quality review runs twice.** Once now on the Phase B sheets for baseline and calibration against the hand-authored library; again on the frozen production generation stack as the launch gate (`CHANGELOG-v6.md`, open condition 2). The first run informs the production prompt and model; it does not close the launch gate.
6. **Stable collaborator anchors are contractual.** The production renderer exposes semantic section identifiers (hero, details, rsvp, registry, …) from its first port so Creation Mode controls attach to slots, never to inferred DOM shape.
7. **Capabilities, ContentProfile and FeaturePresentationState are pure derivation functions from the start**, fed sparse data in early phases. Never hardcoded defaults. Semantics are settled in `spec.md §0b` and §11.4: capabilities = enabled features; content profile = present content; presentation state = guest visibility.
8. **Required details are captured early, during generation**, as publish requirements with bounded provisional content and deterministic re-fit (`spec.md §7.3`, §0b). Phase 2 owns the minimal flow so Phase 4 never builds a throwaway onboarding UI.

## Sequence

| Phase | Scope | Exit condition |
| --- | --- | --- |
| **0. Scaffolding, deployment, geometry-runtime spike** | Real Next.js App Router + TypeScript app structure, Supabase project/config, environment handling, server/client boundaries, shared UI tokens, lint/typecheck/test, CI, Vercel preview deployment, basic route shells. **Spike:** can Vercel run the production renderer + fonts + Chromium at 390 and 1280 reliably within acceptable bundle size, cold start, execution time and cost? Go: lock Vercel serverless Chromium. No-go: adopt the documented render-worker option. **In parallel:** human design test #1 on the existing Phase B sheets (`../proof-b/human-test-form.md`, `score-human.js`), calibrated against the library in the same session. | Deploys from CI; spike verdict written into `technology-decisions.md`; human test #1 scored and recorded. **Status 2026-09-12:** scaffold, CI and Phase 1 foundation merged (#4); Vercel project `event-platform` deploys `main` and every PR (preview `https://event-platform-git-claude-phase-5ca18b-haseeb-shakers-projects.vercel.app`, production `https://event-platform-two-rho.vercel.app`); spike verdict **GO — Vercel serverless Chromium**, evidence in `docs/spike/` (`vercel-warm.json`, `vercel-cold.json`, `vercel-first-cold.json`, local series), recorded in `technology-decisions.md`; human test #1 prepared in `docs/human-test-1/` and hosted as an online survey at `/human-test-1` (PR #9). **Status 2026-09-14: stopped early by decision.** It is qualitative calibration evidence, not a pass/fail gate, and no pass/fail claim is made; the frozen assets and scorer are unchanged. The qualitative evidence is recorded canonically in `human-test-1/qualitative-findings.md` and drives Phase 3.1. Human test #2 remains the launch gate. |
| **1. Core data, auth and security foundation** | Only what every later domain depends on: account/auth linkage, Event, ownership and collaborators, event draft state, generation and concept persistence, versioned immutable generated artifacts (`ResolvedDesignSpec` revisions, `activeResolvedSpecId`, `contentVersion`), RLS foundations, stable identifiers. Supabase Auth for hosts and co-hosts; server-side authorization helpers. Signup abuse throttling begins here. | Migrations and RLS tested; authorization helpers covered; no RSVP, registry, messaging or payment tables yet. |
| **2. Prompt → auth/save → minimal missing-details flow** | Landing prompt, optional inspiration upload with persistence, pre-auth draft persistence that survives the auth redirect, OAuth, restoration after auth. Minimal required-details capture (date, time, venue, hosts, RSVP deadline, visibility) usable while generation runs; end time optional; RSVP deadline default per `spec.md`. | First true end-to-end slice: describe event → authenticate → draft exists with details captured. **Status: complete, merged.** PR #6 (prompt → auth → details) and PR #7 (concurrency follow-up) are both on `main`, which is the current Phase 2 foundation. Landing composer, private pre-auth draft (opaque cookie token, HMAC-hashed server side), optional upload-only inspiration in a private bucket, email and OAuth sign-in, and an atomic `claim_pre_auth_draft` that is idempotent under retried or concurrent callbacks. Events can only be created server-side by that claim. Required details are collected as §23.1 publish requirements, never as generation prerequisites; the RSVP deadline follows the canonical rule and stops recomputing once the host edits it; the timezone is inferred from venue text with a browser fallback and no geocoder. Abandoned drafts and their objects expire on a daily schedule. PR #7 added optimistic concurrency to `updateEventDetails()`: a monotonic `row_version` with a compare-and-set update and a bounded retry, so concurrent autosaves cannot lose a write. Verified across both PRs by unit, database and browser tests at 390 and 1280, production build, the proof-b regression, and independent senior review; the persisted path was additionally exercised against the live deployment and database before merge. |
| **3. Production CompositionTree engine** | Port from `../proof-b/` without behaviour change: validator and strict schema, deterministic repair by kind, attractive-token caps, canonicalization, sibling planner and directives, skeleton signature, primitive renderer, page system, palette compiler, typography, motifs, layout resolution, rendered-geometry verification at 390/1280 on the Phase 0 runtime, responsive behaviour, capability scoping, re-fit on content edits, stable section anchors. Regression suite: unit tests, adversarial fixtures, the 72 frozen trees. No live AI. Can run in parallel with Phases 1–2 when more than one agent is available. | All `event-renderer-system.md §9` thresholds met on the production code; confirmation set re-verified clean at both widths.  **Status: complete. Implemented in PR #8.** Production CompositionTree language, compiler and renderer, with authoritative rendered-geometry verification. Evidence: 72/72 accepted frozen confirmation trees geometry-clean at 390 and 1280; 12/12 reduced-capability trees clean with no disabled feature rendered; a novel CompositionTree validates, compiles, renders and verifies clean end to end without library selection, with both legacy-library adapters mocked to throw; content re-fit produces a new immutable revision with the same canonical tree and `compositionHash`, an incremented `contentVersion` and `supersedesSpecId`; the production CompositionTree JSON Schema is generated from `NODE_SPEC` — the same table the validator and prompt text come from — and mechanically checked against the committed artifact; geometry verification confirmed on the deployed Vercel serverless Chromium runtime. 26 hero silhouettes, 13 section recipes and 16 A.1 pages all render through the one renderer with no recipe-specific path. No live model calls were made. Human design test #1 was subsequently stopped early and is qualitative calibration evidence only (`human-test-1/qualitative-findings.md`); it was never a Phase 3 gate. Its findings F1 and F2 are the scope of Phase 3.1 below. |
| **3.1. Deterministic visual-quality hardening** | Bounded remediation of Human Test #1 findings F1 and F2 (`human-test-1/qualitative-findings.md`). F1 in full: real rendered line-box measurement in geometry verification, minimum usable text measures, line budgets for secondary metadata, removal of pathological mid-word heading breaks, alignment-edge coherence for normal multiline text, orphan protection, and stagger/cascade line breaking that cooperates with the treatment instead of fixed word slicing followed by browser wrapping. F2 only as a narrow measured hierarchy safeguard, and only if it fits the existing motif/hierarchy infrastructure cleanly — defer it rather than build a generalized visual-ranking engine. **No generated imagery. No model calls. Prefer no CompositionTree schema change, no new primitives, props or tokens.** | All `event-renderer-system.md §9` gates re-run and met: unit tests, the adversarial set, the library expressiveness render, and a fresh sibling-batch confirmation run. The F1 examples no longer reproduce at 390 and 1280. **Status: complete for F1; F2 deferred to Phase 4.** Syntactic title breaking replaces the fixed 0–1 / 2–3 / rest word slice; `stagger` no longer flips a line's alignment — it indents the odd lines and `cascade` ramps from the second, so the two stay distinct on the two-line titles that are the majority — and both offsets are bounded lengths in `em` rather than percentages, because CSS resolves percentage padding to zero during intrinsic sizing and a shrink-wrapped title then loses exactly the indent from its longest line and breaks inside a word; headings take `break-word` + `text-wrap: balance` and body/metadata `text-wrap: pretty`; geometry verification measures real line boxes and gates on three new defects — text broken inside an unbreakable segment, atomic metadata past a word-scaled line budget, and incoherent line edges — the first repaired by the existing demote-then-relax ladder and the second by relaxation alone, since its budget applies at and below the demotion floor. No schema change, no new primitive/prop/token, no model call. `COMPILER_VERSION` bumped to `compiler_phase3_1_0`, since the clean criterion, the overrides produced for identical input and the persisted `verified` record all changed. Gates re-run and met: 737 unit tests including the 72/72 frozen confirmation trees clean through production geometry, `proof-b/test.js`, `proof-b/adv-run.js` 37/37 with zero overflow — noting that the two proof-b harnesses carry their own renderer and verifier and exercise none of the changed code, so the production replay and a 13-title × 3-treatment content sweep are the gates that can see it. Frozen 32 and 51 were clean on the old criterion and are not on the new one; both repair to clean and are pinned as named regression cases at 390 and 1280 in `src/lib/renderer/verify/typography.test.ts`. F2 deferred with reasons in `human-test-1/qualitative-findings.md`. Note: §9's thresholds include a human design-quality rate; hardening changes rendered geometry, so Human Test #1 is a *before* measurement and Human Test #2 carries the gate. |
| **4. AI generation system** | Event Identity → sibling planner → 3 DesignIntents → 3 CompositionTrees → Phase 3 compiler → ResolvedDesignSpecs. Prompt/schema/primitive-set/compiler versioning, one re-prompt per allowed reason, telemetry, idempotency, retries and fallbacks, generation state, spend controls (principle 4) before the creative endpoint is exposed. Capability/content-profile derivation wired in. Production-model quality evaluation against the frozen thresholds. **Approved on Human Test #1 evidence (F3, F4), `../spec.md §7.6a`:** optional AI-generated thematic artwork — visual-anchor selection, art-brief generation, an image-generation pipeline, composition-aware placement, latency/cost strategy and fallback when generation fails. Imagery is optional, art-directed and compiler-placed; it is never mandatory and never model-positioned, and the image model and artwork schema are still unselected. **Also approved, `../spec.md §7.6b`:** adaptive clarification on two routes — Route A creative, bounded, taste-only, never logistics, always a `You decide` option, never a gate on concepts appearing; Route B authority/boundary, rare, asked alone, no `You decide` option, and the only route permitted to block concepts. **Read `product-doctrine.md` before any creative decision in this phase**: it states the product promise, the quality bar, `EventIdentity`'s responsibility as the creative interpreter, the fact-versus-interpretation boundary, the doctrine on adaptive creative clarification, and the priority order this phase must not invert (understand first, optimise cost and latency second). | **Operational:** uncontrolled model calls impossible; confirmation-run metrics reproduced on the production stack. **Creative — Phase 4 is not complete on valid JSON, validating schemas, three compiling trees and acceptable latency alone.** It additionally requires evidence, against `model-contracts.md §4.5` and its corpus, that: EventIdentity understands representative vague and taste-heavy prompts; it invents no event fact the host did not supply; adaptive clarification behaves selectively — nothing in the common case, never logistics, within the ceiling; DesignIntent produces materially distinct directions rather than palette or font swaps; all three stay faithful to the interpreted identity; and the user-facing result reaches *personalization rather than rescue*. Thresholds are calibrated when the corpus is first run against a real model, not invented now. Human Test #2 (Phase 10) remains the final launch-quality human gate and this criterion does not replace it. |
| **5. Concept choice, reveal and Creation Mode** | Generation progress → three concept directions → selection → full-site reveal → "Make it yours" → the event itself as the editing workspace. Contextual Edit/Set up/Add controls on the section anchors, autosave, design controls, preview, pre-publish redesign. Not a setup wizard (`design-system.md`). **The generation surface is part of this phase's product, not a spinner** (`../spec.md §7.10`, `product-doctrine.md §8a`): concept-level readiness so no concept waits on its siblings, only real pipeline artifacts shown, never model reasoning or fabricated progress, and optional non-blocking entry of missing event facts while generation runs. | Host can go from prompt to an edited, previewable site with no wizard. |
| **6. Full event details, readiness, privacy, collaboration** | Full details editor, required-vs-recommended readiness state (`spec.md §19.2`), private/public settings and access code, owner/co-host permissions and invitations. | Host-side creation functionally complete. |
| **7. Guests and RSVP** | Manual entry and CSV, parties/households, needs-phone, invite lookup, Twilio OTP, guest sessions (no Supabase accounts for guests), attendance/plus-one/questions, validation, confirmation and update flow, host guest-management workspace. RSVP tables arrive here. RSVP becomes guest-visible once configured and at least one party is invited. | RSVP end to end at event and account limits. |
| **8. Registry** | External registries, individual gift URLs and metadata, normalized product images, display-only cash fund, Available/Purchased state, click logging, return self-confirmation, host correction tools. Registry tables arrive here. | Registry guest-visible once it has an item, external link or cash fund. |
| **9. Publish, payment boundary, live site, Management Mode** | $49 Stripe-shaped mock payment boundary, READY_TO_PUBLISH enforcement, publish, subdomain/live routing, public/private guest site, sharing, Passed state; management home: RSVP summary → Guests → Messages → Registry → Share → Edit site. No billing architecture beyond the stub. Payment state tables arrive here. | Live event reachable and manageable. |
| **10. Messaging, hardening, launch** | SMS reminders and announcements, STOP handling, rate limits, observability, accessibility, security review, E2E coverage, responsive and visual regression, performance, failure-state QA. **Human design test #2** on the frozen production generation stack: the launch gate, and the only human design-quality gate. **Its protocol and pass threshold are frozen and recorded before the results are reviewed** — calibration against the library happens in the same session, but the bar is never chosen or moved after the outcome is known (`../spec.md §11.9`). It must cover design quality, diversity, theme fidelity, typography credibility, visual hierarchy, whether any imagery feels bespoke rather than generic, and whether image-free concepts still feel intentional (`human-test-1/qualitative-findings.md`). Messaging tables arrive here. | Launch checklist and human test #2 pass. |

## Phase 4 sub-phases

Phase 4 is gated rather than continuous. Each sub-phase gets its own branch, PR, validation,
independent review and product-quality checkpoint, and the next does not begin until the
previous one is judged good enough — because every stage after 4A consumes 4A's
understanding, so shipping a weak interpreter compounds rather than shows up later.

| | Core question | Scope |
| --- | --- | --- |
| **4A** | Does the AI actually understand what the host means? | The production `EventIdentity` call and its evaluation against `model-contracts.md §4.5`. No DesignIntent, no composition, no imagery, no generation UI |
| **4B** | Does it know when it needs to ask something? | The 0–3 contextual creative-question loop, `You decide`, identity refinement after an answer, and the minimal surface (`spec.md §7.6b`). **Binding obligations carried from v5, none of which Phase 4A implements:** (a) a boundary question blocks concept generation and the identity returned beside it is provisional — it must not reach the sibling planner, DesignIntent, composition generation or any downstream creative stage (`spec.md §7.6b`, §7.7); (b) after the host answers a boundary question, **Event Identity must run again**, and only a result carrying no boundary question may become the authoritative identity — if the rerun returns another boundary question it is provisional and blocked in turn; (c) **clarification-answer input provenance** — a clarification answer is first-class current host input on the rerun: it is preserved separately from the original event description, takes precedence over conflicting earlier host input, stays attributable to the host, is never implemented by silently mutating or concatenating the original prompt, is never overloaded into `redesignFeedback`, and its concrete input envelope and persistence semantics are defined before the clarification loop is built; (d) repeated boundary rounds are permitted and are an orchestration/telemetry concern — there is no lifetime cap |
| **4C** | Can it invent three excellent, genuinely different directions from one identity? | `DesignIntent × 3` through the sibling planner. Different creative interpretations, never palette or font swaps |
| **4D** | Can it turn each direction into an excellent original composition? | Model-authored `CompositionTree`s onto the Phase 3/3.1 compiler, verifier, repair, RDS and renderer. The first real generated websites to judge |
| **4E** | Can art-directed imagery raise the design ceiling? | `VisualArtIntent` and generated thematic artwork (`spec.md §7.6a`): image-model selection, transparent-background reliability, illustrative anchors, ambient art, composition-aware negative space, crop safety, asset persistence, failure fallback, originality. Artwork stays optional |
| **4F** | Does creating the event feel like watching your idea come to life? | The real progress experience over actual pipeline artifacts (`spec.md §7.10`, `product-doctrine.md §8a`): interpreted vibe, palette territory, visual vocabulary, concept names, art direction, progressive and independent concept readiness, optional detail entry while generation continues. No fake reasoning, no fake percentages, no generic spinner |
| **4G** | Does the whole thing deliver personalization rather than rescue? | End to end: prompt → identity → clarification if needed → three directions → compositions → artwork where appropriate → responsive verified sites → selection → send-ready. **Human Test #2's protocol and threshold are frozen and recorded here, before any HT2 result is seen** (`spec.md §11.9`) |

**4A status: CLOSED — GO (2026-09-15).** `event_identity_v5` / `event_identity_schema_v5` is the
accepted production interpretation contract. Six live runs happened in all: `v3` against the
fourteen-case regression suite (13/14 mechanical) and `v4` against the first sealed challenge
(11/12), both of which **failed** their independent qualitative gate and produced the `v4` and `v5`
remediations; then four at `v5` — regression 14/14, the spent-challenge diagnostic 11/12, the
pre-registered holdout 12/12, and the fresh sealed challenge `sealed_challenge_v2` 11/12 with a
blind review of 7 Excellent, 5 Good, 0 Borderline, 0 Fail. The go/no-go record and its six SHAs are
in `model-contracts.md §4.6`. **Phase 4B is unblocked. No `v5` remediation precedes it.** Every eval
set is now spent — `sealed_challenge_v1` was already spent at `v4`, and `sealed_challenge_v2` is
spent by the run that carried the GO — so a future prompt version needs a newly authored sealed
corpus and its own slot; no existing set can stand in.

The four qualities the blind review kept noticing are carried as **excellence watch** items through
DesignIntent, Composition and rendered-concept evaluation (`model-contracts.md §4.6`), not as `v5`
defects. `v5`'s ~30 s median is recorded as **latency debt**, addressed after the creative pipeline
is proven (`spec.md §7.10`). Neither is authorization to reopen `v5`.

The history below is preserved as written. It records what was true when each run happened.

The fourteen-case run against `gpt-5.6-sol` was executed on 2026-09-14 at implementation `ba34c5e`.
Mechanical evidence was collected and is preserved unchanged in
`../docs/model-evals/results/creative-understanding-v1/`: 14/14 structured output, 13/14 mechanical
pass, p50 latency 24.6 s. The one mechanical failure (CU-05) is an evaluator false positive,
recorded rather than repaired, and the original result stands.

**The independent qualitative review failed the gate**
(`creative-understanding-v1/astra-qualitative-review.md`). The mechanical run was measuring a
boundary the corpus never drew: the model routinely converts its own taste into constraints
*attributed to the host* — roughly 39 of the 55 constraints the run produced are model taste
wearing host authority. That is not a quality problem but a correctness one, of the same kind as
inventing a fact, and every later stage reads this object and cannot tell an invented prohibition
from a real one. Three further findings: an overly strong house taste that equates sophistication
with abstraction, zero clarifications across all fourteen cases, and briefs that describe
originality rather than supplying it.

This is exactly the failure `model-contracts.md §4.5` predicted when it said a mechanical pass is
"necessary and never sufficient", and exactly why the human gate exists.

Remediation, approved as a source-of-truth change: split `designConstraints` into authoritative
`hostConstraints` (grounded in an explicit host phrase) and advisory `creativeGuidance`; add a
verbatim honoree-context field; rewrite the prompt's originality section so sophistication is a
property of execution rather than abstraction; and make the clarification rule decidable.

**The original fourteen cases are now a regression suite, not fresh evidence** — every output has
been inspected and discussed. Three evidence classes follow, and they are not interchangeable:
the regression suite; the **pre-registered validation set** of twelve cases, authored and frozen
*before* remediation and independently reviewed, but written by the same person who then wrote
the prompt, so it is validation and not generalization; and the **sealed challenge**, authored
independently after the implementation was frozen, which is the only class that can carry
generalization. Its runner path is wired and frozen ahead of its cases, so that seeing them cannot
be followed by adjusting the harness that grades them.

**`sealed_challenge_v1` is spent.** The first sealed corpus was run once, at `v4`, on 2026-09-15
(`175eefd`), and failed its independent qualitative gate. `v5` was written with those cases known,
so from `v5` onward that corpus is a **regression set and never generalization evidence again** —
a clean re-run of it says only that `v5` did not break what `v4` did. Fresh generalization evidence
for `v5` required a **new independently authored sealed corpus**, unseen while `v5` was written.
That corpus is `sealed_challenge_v2`: authored after the harness froze at `19f1ec8`, input-frozen at
`9053b6d` in a commit that changed one file, run once, and now spent in turn. The gate was applied
as written — deterministic checks, independent engineering review, an independent qualitative read
of a blind artifact, then an explicit go/no-go — and it returned **GO**.

**The plan for what follows is `phase-4b-plan.md`.** It decomposes **4B** (the clarification
lifecycle, answer provenance and the minimal surface) and **4C** (the deterministic sibling planner
and `DesignIntent × 3`) into auditable tasks, and keeps the letters above exactly as they are: the
two may be built as one workstream but are judged separately, and **4C does not begin until 4B has
independently passed its gate**. It fixes the point at which approval is required before the first
live DesignIntent call, states both exit gates — including a systemic-veto rule for 4C that fails
the phase regardless of rating distribution — and designs each phase's evidence strategy before its
prompt is written. It orders work and defines no requirements.

## Recorded deviations

- **`docs/product-doctrine.md §6` was reconciled to `v5`, by explicit approval.** Doctrine had said clarification was "about taste only" and that every question always offers `You decide` — true of `v4`, false of `v5`. §6 now carries both routes, §4's flow no longer implies all clarification is creative, §7 names Route B as the narrow authority exception rather than a relaxation of the logistics or design-decision bans, and §14 conflict 2 records the original `v4` decision and its `v5` expansion as an audit trail. The durable principle — *understand aggressively, infer creatively, ask selectively* — is unchanged. Resolved, not open.


- **`spec.md §31 — Prompt, auth, and generation`: "Required details are collected only when missing and while generation runs, and never block concepts from appearing."** Phase 2 satisfies every part of this except "while generation runs", which it cannot: generation is Phase 4 and the AI provider is deliberately unimplemented until then. Phase 2 collects the details anyway, exactly as the criterion intends, and the generation surface states plainly that no design has been generated yet rather than narrating work that is not happening. The bullet is checkable once Phase 4 lands; until then it is knowingly partial, not met. Recorded here so no later reader mistakes the Phase 2 screen's honesty for a regression, and so nobody papers over it with copy that implies generation is running.

## Decisions that must not be reopened by this plan

- The Phase 3/4 order (engine before AI) and the Phase 0 spike as a go/no-go.
- Client-side geometry verification is rejected as a casual fallback.
- Capability semantics and early required-details capture are settled in Revision 6.1; implementation follows `spec.md §0b`, not this document.
- The locked stack in `technology-decisions.md`.

## Maintaining this document

Update the exit-condition column with dates and evidence links as phases close. Record the Phase 0 spike verdict in `technology-decisions.md` and cite it here. Do not add product requirements here; propose them against `spec.md`.
